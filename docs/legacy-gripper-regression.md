# 旧简化夹爪测试回归记录

这条记录只定位已有简化夹爪分支的回归，不扩展该演示，也不把它当作 PiPER 验收。

| 代码版本 | 命令 | 结果 |
|---|---|---|
| `717557d`（M1 之前） | `PYTHONPATH=src python3 -m pytest tests/test_mujoco_simulation.py::test_physics_pick_place_from_second_start -q` | 1 passed |
| `9a51151`（M1 提交） | 同上 | 1 failed，`grasp_contact=False` |

回归首次出现在 `9a51151`，不是后续补修引入。该提交把旧夹爪从 `gripper` body
下的两个滑动关节改成世界中的两个独立 mocap body，并改变了指尖几何与位置更新方式。
因此旧测试使用 `(0.25, -0.10)` 起点时，闭合阶段无法同时形成原测试所需的两对接触；
方块仍保持自由刚体，失败结果是真实接触检测结果。默认起点的旧测试仍按原记录运行。

从 9a51151 起，这个适配器只保留作历史接触测试；项目主演示是完整 PiPER。
不通过继续增加漂浮夹爪摩擦、挤压力或脚本分支来修复这条回归。若以后需要修复，
应另建隔离实验并重新定义运动学/动力学验收，不能改变 PiPER M1/M2 的证据。
